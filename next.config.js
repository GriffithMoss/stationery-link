/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      // Add more domains here if needed
    ],
  },
};

module.exports = nextConfig;
